name: investerei_app
description: InvesteRei mobile client (Android + iOS)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/investerei-logo.png
