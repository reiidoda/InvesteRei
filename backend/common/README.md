# Common

In a larger build you would publish this as an internal library (Gradle multi-module).
For MVP simplicity, this repo uses copy-paste minimal DTOs + error formats per service.

Recommended next step:
- Convert to a Gradle multi-module build with shared 'common' module.
